# 작업 리스트

각 작업은 **검증 가능한 완료조건**을 갖는다 ([CLAUDE.md](../CLAUDE.md) 원칙 4).
완료조건을 눈으로 확인하기 전에는 체크하지 않는다.

상태: `[ ]` 대기 · `[~]` 진행중 · `[x]` 완료 · `[!]` 막힘(사용자 결정 필요)

---

## Phase 0 · 프로젝트 세팅

- [x] **T-001** 저장소 구조·문서·설정 생성
- [x] **T-002** taste-skill 을 `.claude/skills/` 에 설치
- [x] **T-003** Python 환경 — geopandas·shapely·pyproj·requests·dotenv 동작 확인
  - rasterio·osmnx·elevation은 D-008/D-010으로 불필요해져 미설치
- [ ] **T-004** 타일 툴체인 설치 — `brew install tippecanoe`
  - 완료조건: `tippecanoe --version` 출력 확인
- [ ] **T-005** 첫 커밋 및 GitHub 푸시
  - 완료조건: `git log` 에 커밋 1개, `git push` 성공

---

## Phase 1 · 데이터 수집

- [x] **T-101** 서울 열린데이터광장 인증키 발급 → `.env` — 784건 수신 확인
- [x] **T-102a** 토지특성정보(AL_D194) 성북구 확보 — 52,970 필지, 기준일 2026-05-13
- [x] **T-103** 서울시 도시계획조례 제44·48조 원문 대조 검증 — `far_bcr_seoul.csv`
- [x] **T-102b** GIS건물통합정보(AL_D010) 확보 — 서울 695,761동 / 성북구 **40,368동**, 기준일 2026-08-09
  - 높이 = `A16`, 지상층수 = `A26` (실증 확인). 층고 실측 중위 3.25m
  - ⚠️ **높이·층수 둘 다 결측 10,009동(24.8%)** → Q-09 결정 필요
- [x] **T-104** 지하철역 수집 — `scripts/03_stations.py`
  - ✅ 784건 수신 → 성북구 내 13개 + 버퍼 1.5km 35개 = **48개 지점**
- [x] ~~T-105 OSM 도로망~~ — A24 도로접면으로 대체, 불필요 (D-010)
- [x] ~~T-106 대학 좌표~~ / ~~T-107 DEM~~ — 4순위 지표 미채택으로 불필요 (D-008)

## Phase 2 · 전처리 · 필지 마스터 구축

- [x] **T-201** 토지특성정보 로드 + PNU·좌표계 검증 — `scripts/01_build_parcels.py`
  - ✅ 52,970행, PNU 19자리·중복 0·시군구 일치, EPSG:5186
- [x] **T-202** 컬럼 매핑 — 컬럼정의서.xlsx 원문 기준 (`scripts/common.py`)
  - A16은 "지정지구"가 아니라 **용도지역명2**. A20/A22/A24 정식명은 지형높이/지형형상/도로측면
- [x] **T-203** 용도지역 → 용적률·건폐율·기숙사가능 매핑
  - ✅ 2종일반 52.5% / 3종 17.7% / 1종 14.9% 재현, 미매핑 용도지역 0
  - 용도지역 2곳에 걸친 필지 3,472건 → zone1 채택 + `W_ZONE2` 플래그
- [x] **T-204** 건물 높이 정규화 — `scripts/04_solar.py`
  - ✅ 실측 13,666(33.9%) / 층수추정 16,693(41.4%) / **면적추정** 10,009(24.8%)
  - ✅ 음수 높이 1동 결측 처리, 200m 초과 0건. 미상 건물은 D-012 바닥면적 추정
- [x] **T-205** 필지 마스터 → `data/interim/parcels.gpkg`
  - ✅ 52,970행, 필수 컬럼 결측 0 (공시지가 0원 19건은 `F_PRICE` 플래그로 제외 처리)
- [x] **T-206** AL_D151 개별공시지가 조인 — 조인율 **100%** (52,951/52,970)
  - **결과: A25와 값이 전부 동일 (상이 0건).** 기준일 차이는 추출일이었을 뿐
    공시 자체는 같은 2026-04-30 건이다. AL_D151 다운로드는 사실상 불필요했다.
  - `price_source` 컬럼에 출처 기록

---

## Phase 3 · 지표 계산

> 파라미터·수식은 [METHODOLOGY.md](METHODOLOGY.md) 와 `config/params.yaml` 을 따른다.
> 아래 기대값은 이번 세션에서 실제로 계산해 확인한 수치다. **재현되지 않으면 버그다.**

- [x] **T-301** 하드 필터 → `flags` 비트마스크 — `scripts/02_metrics_s1.py`
  - ✅ 퍼널 52,970 → **4,896** (D-011 적용 후. 적용 전 5,238)
  - 부수 발견: 도로측면 미지정 12,123건 중 후보에 남은 건 **단 2건** → Q-07 종결
- [x] **T-302** S₁ 사업성 (순투자금·총사업비·토지비 3종)
  - ✅ 순투자금 중위 **4.0%** (2.7~5.0%), 실현계수 중위 **0.83**
  - ✅ 건전성: 공시지가 상관 **−0.74** / 용적률 상관 **+0.01**
  - D-011(상업지역 주거 400% 상한) 적용으로 순위 왜곡 수정됨
- [x] **T-303** S₂ 일조환경 — 남측 섹터 앙각 개방도
  - ✅ **0.6초** (목표 10분). 이웃 쌍 161만, STRtree + numpy 벡터화
  - ✅ 접도 등급별 S₂가 **보너스 없이도** 단조 증가 (광대소각 0.854 → 세로각지 0.536)
  - ✅ 개방 상위=성북동 저밀지 / 차폐 하위=동선동·보문동 역세권 밀집지
- [x] **T-304** S₃ 역세권 등급 (직선거리, A~E)
  - ✅ 한성대입구·고려대·길음 250m 내 필지 전부 A등급 (위반 0)
  - 후보 분포 A 1,026 / B 2,062 / C 1,189 / D 345 / E 274, 역거리 중위 420m
  - **거리는 필지 대표점 기준** (D-013)
- [x] **T-305** 계층 정렬 (백분위 밴드, tol 5/10) — `scripts/05_ranking.py`
  - ✅ 회귀: `tol₁=0` → S₁ 단조 비증가 위반 **0건** (동점 282건은 S₂로 타이브레이크, D-013)
  - ✅ tol₁ 민감도: 0→200/200, 2→196, **5→162**, 10→89, 20→55
  - 상위200: S₁ 5.11%(전체 3.98%), 3종일반 73 / 2종일반 67 / 준주거 46
- [ ] **T-306** 상위 20필지 지도 육안 검증 **(사용자와 함께)** ← **다음 차례**
  - 완료조건: 명백히 부적합한 필지 없음. 있으면 원인 규명 후 지표 수정
  - 현재 상위: 하월곡동·정릉동·장위동·성북동. 사업비 25~103억, 21~90실

## Phase 4 · 타일 빌드

- [ ] **T-401** raw 속성만 담은 GeoJSON 내보내기 (EPSG:4326)
  - 담을 속성: [METHODOLOGY.md §6](METHODOLOGY.md) 목록 준수. **사전 계산된 점수는 넣지 않는다**
- [ ] **T-402** tippecanoe → `web/data/parcels.pmtiles`
  - 완료조건: 파일 < 50MB, z14~z17에서 필지 경계가 뭉개지지 않음
- [ ] **T-403** 부가 레이어 (지하철역 포인트, 구 경계) GeoJSON — 소형이라 타일 불필요

---

## Phase 5 · 웹 구현

> 디자인은 `.claude/skills/taste-skill/SKILL.md` 를 따른다.
> 단, 이 스킬은 랜딩·포트폴리오용이며 **대시보드는 명시적으로 제외 범위**다.
> 시각 언어(타이포·컬러·모션·안티슬롭 규칙)만 차용하고, 레이아웃은 지도 대시보드 관례를 따른다.
> 차트·색상은 `dataviz` 스킬을 함께 참조한다.

- [ ] **T-501** 지도 셸 — MapLibre + PMTiles 로드
  - 완료조건: 로컬 서버에서 성북구 필지가 렌더링됨
- [ ] **T-502** 브라우저 점수 엔진 (JS) — raw 속성에서 S₁~S₃ + 계층 정렬 재계산
  - 완료조건: **동일 파라미터에서 Python 결과와 순위가 일치** (상위 100개 대조)
  - 이 검증을 건너뛰면 웹이 조용히 틀린 답을 보여준다
- [ ] **T-503** 파라미터 패널 (슬라이더·토글) — §6 표의 항목
  - 완료조건: 슬라이더 조작 시 200ms 이내 지도 색상 갱신
- [ ] **T-504** 필지 클릭 → 상세 패널 (지표별 값·순위·제외사유)
- [ ] **T-505** 상위 후보 리스트 + 리스트↔지도 연동
- [ ] **T-506** 가정값 경고 배너 — 실측값과 가정값을 구분해 표시 (ROADMAP 하단 표 참조)
- [ ] **T-507** 반응형 + 다크모드 + 접근성 대비 확인

---

## Phase 6 · 배포

- [ ] **T-601** GitHub Pages 설정 (`web/` 또는 `gh-pages` 브랜치)
  - 완료조건: 공개 URL에서 지도가 정상 동작. **과금 서비스 0개**
- [ ] **T-602** PMTiles Range 요청이 Pages에서 동작하는지 실배포 확인
  - GitHub Pages는 Range를 지원하지만 **실제 URL에서 반드시 확인**한다
- [ ] **T-603** README에 방법론 요약 + 한계 명시
- [ ] **T-604** 데이터 출처·기준연도·라이선스 표기 (푸터)

---

## 지금 막혀 있는 것

**데이터는 전부 확보됐다.** 이제 구현만 남았다.

**Phase 3 계산이 전부 끝났다.** 다음은 **T-306 (사용자와 상위 필지 육안 검증)** 이고,
그 뒤 Phase 4(타일 빌드) → Phase 5(웹) 로 간다.

1. **T-306** — 상위 20필지를 지도에서 함께 확인. 지표가 바뀔 수 있는 마지막 지점이다.
2. **Q-11** — 상위 6위가 역세권 E등급(1,577m)이다. 계층 정렬 설계상 정상이지만
   실사용 시 역거리 하한을 둘지 판단이 필요하다.
3. ~~Q-07~~ / ~~Q-08~~ / ~~Q-09~~ / ~~Q-10~~ — 전부 종결

## 아직 검증되지 않은 가정

결과를 의사결정에 쓰기 전에 반드시 확인해야 한다:

| 파라미터 | 현재값 | 상태 |
|----------|--------|------|
| 단위공사비 | 250만원/㎡ | **가정** — 임대형기숙사 실제 시공 견적 대조 필요 |
| 전용률 | 0.65 | **가정** |
| 실당 면적 | 16㎡ | **가정** |
| 공실률 / 운영비율 | 0.05 / 0.25 | **가정** |
| 실거래배율 | 1.5 | **가정** (통상 1.4~1.7) |
| 실현계수 base | 0.90 | **가정** (형상·경사 계수는 실측) |
| 실당 월임대료 / 보증금 | 66만 / 1,000만원 | ✅ 2026년 성북구 시세 |
| 용적률·건폐율 | 조례표 | ✅ 조례 원문 |
| 용도지역·형상·경사·공시지가·접도 | — | ✅ 토지특성정보 실측 |
| 상업지역 주거용 400% 상한 | — | ✅ 조례 별표 (D-011) |
