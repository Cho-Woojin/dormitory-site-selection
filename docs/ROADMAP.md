# 작업 리스트

각 작업은 **검증 가능한 완료조건**을 갖는다 ([CLAUDE.md](../CLAUDE.md) 원칙 4).
완료조건을 눈으로 확인하기 전에는 체크하지 않는다.

상태: `[ ]` 대기 · `[~]` 진행중 · `[x]` 완료 · `[!]` 막힘(사용자 결정 필요)

---

## Phase 0 · 프로젝트 세팅

- [x] **T-001** 저장소 구조·문서·설정 생성
- [x] **T-002** taste-skill 을 `.claude/skills/` 에 설치
- [x] **T-003** Python 환경 — geopandas·shapely·pyproj·requests·dotenv 동작 확인
  - rasterio·osmnx·elevation은 D-008/D-010으로 불필요해져 미설치
- [x] **T-004** 타일 툴체인 — tippecanoe v2.79.0
- [x] **T-005** GitHub 푸시 — `Cho-Woojin/dormitory-site-selection` (public)

---

## Phase 1 · 데이터 수집

- [x] **T-101** 서울 열린데이터광장 인증키 발급 → `.env` — 784건 수신 확인
- [x] **T-102a** 토지특성정보(AL_D194) 성북구 확보 — 52,970 필지, 기준일 2026-05-13
- [x] **T-103** 서울시 도시계획조례 제44·48조 원문 대조 검증 — `far_bcr_seoul.csv`
- [x] **T-102b** GIS건물통합정보(AL_D010) 확보 — 서울 695,761동 / 성북구 **40,368동**, 기준일 2026-08-09
  - 높이 = `A16`, 지상층수 = `A26` (실증 확인). 층고 실측 중위 3.25m
  - ⚠️ **높이·층수 둘 다 결측 10,009동(24.8%)** → Q-09 결정 필요
- [x] **T-104** 지하철역 수집 — `scripts/03_stations.py`
  - ✅ 784건 수신 → 성북구 내 13개 + 버퍼 1.5km 35개 = **48개 지점**
- [x] ~~T-105 OSM 도로망~~ — A24 도로접면으로 대체, 불필요 (D-010)
- [x] ~~T-106 대학 좌표~~ / ~~T-107 DEM~~ — 4순위 지표 미채택으로 불필요 (D-008)

## Phase 2 · 전처리 · 필지 마스터 구축

- [x] **T-201** 토지특성정보 로드 + PNU·좌표계 검증 — `scripts/01_build_parcels.py`
  - ✅ 52,970행, PNU 19자리·중복 0·시군구 일치, EPSG:5186
- [x] **T-202** 컬럼 매핑 — 컬럼정의서.xlsx 원문 기준 (`scripts/common.py`)
  - A16은 "지정지구"가 아니라 **용도지역명2**. A20/A22/A24 정식명은 지형높이/지형형상/도로측면
- [x] **T-203** 용도지역 → 용적률·건폐율·기숙사가능 매핑
  - ✅ 2종일반 52.5% / 3종 17.7% / 1종 14.9% 재현, 미매핑 용도지역 0
  - 용도지역 2곳에 걸친 필지 3,472건 → zone1 채택 + `W_ZONE2` 플래그
- [x] **T-204** 건물 높이 정규화 — `scripts/04_solar.py`
  - ✅ 실측 13,666(33.9%) / 층수추정 16,693(41.4%) / **면적추정** 10,009(24.8%)
  - ✅ 음수 높이 1동 결측 처리, 200m 초과 0건. 미상 건물은 D-012 바닥면적 추정
- [x] **T-205** 필지 마스터 → `data/interim/parcels.gpkg`
  - ✅ 52,970행, 필수 컬럼 결측 0 (공시지가 0원 19건은 `F_PRICE` 플래그로 제외 처리)
- [x] **T-206** AL_D151 개별공시지가 조인 — 조인율 **100%** (52,951/52,970)
  - **결과: A25와 값이 전부 동일 (상이 0건).** 기준일 차이는 추출일이었을 뿐
    공시 자체는 같은 2026-04-30 건이다. AL_D151 다운로드는 사실상 불필요했다.
  - `price_source` 컬럼에 출처 기록

---

## Phase 3 · 지표 계산

> 파라미터·수식은 [METHODOLOGY.md](METHODOLOGY.md) 와 `config/params.yaml` 을 따른다.
> 아래 기대값은 이번 세션에서 실제로 계산해 확인한 수치다. **재현되지 않으면 버그다.**

- [x] **T-301** 하드 필터 → `flags` 비트마스크 — `scripts/02_metrics_s1.py`
  - ✅ 퍼널 52,970 → **4,896** (D-011 적용 후. 적용 전 5,238)
  - 부수 발견: 도로측면 미지정 12,123건 중 후보에 남은 건 **단 2건** → Q-07 종결
- [x] **T-302** S₁ 사업성 (순투자금·총사업비·토지비 3종)
  - ✅ 순투자금 중위 **4.0%** (2.7~5.0%), 실현계수 중위 **0.83**
  - ✅ 건전성: 공시지가 상관 **−0.74** / 용적률 상관 **+0.01**
  - D-011(상업지역 주거 400% 상한) 적용으로 순위 왜곡 수정됨
- [x] **T-303** S₂ 일조환경 — 남측 섹터 앙각 개방도
  - ✅ **0.6초** (목표 10분). 이웃 쌍 161만, STRtree + numpy 벡터화
  - ✅ 접도 등급별 S₂가 **보너스 없이도** 단조 증가 (광대소각 0.854 → 세로각지 0.536)
  - ✅ 개방 상위=성북동 저밀지 / 차폐 하위=동선동·보문동 역세권 밀집지
- [x] **T-304** S₃ 역세권 등급 (직선거리, A~E)
  - ✅ 한성대입구·고려대·길음 250m 내 필지 전부 A등급 (위반 0)
  - 후보 분포 A 1,026 / B 2,062 / C 1,189 / D 345 / E 274, 역거리 중위 420m
  - **거리는 필지 대표점 기준** (D-013)
- [x] **T-305** 계층 정렬 (백분위 밴드, tol 5/10) — `scripts/05_ranking.py`
  - ✅ 회귀: `tol₁=0` → S₁ 단조 비증가 위반 **0건** (동점 282건은 S₂로 타이브레이크, D-013)
  - ✅ tol₁ 민감도: 0→200/200, 2→196, **5→162**, 10→89, 20→55
  - 상위200: S₁ 5.11%(전체 3.98%), 3종일반 73 / 2종일반 67 / 준주거 46
- [x] **T-306** 상위 20필지 검증 — **부적합 3건 발견·수정** (D-014)
  - ❗ 6위가 **하천 부지**였다 → `하천등`·`운동장등` 필터 추가
  - ❗ 후보 90%에 기존 건물이 있는데 **철거비 미반영** → 10만원/㎡ 반영
  - ❗ 다세대(구분소유) 23%가 무표시 통과 → `W_SUBDIVIDED` 플래그 + 웹 토글
  - ✅ 재검증: 부적합 토지이용상황 **0건**, 사업비 25~125억 / 21~100실

## Phase 4 · 타일 빌드

- [x] **T-401** raw 속성 GeoJSON (EPSG:4326) — `scripts/06_export_tiles.py`
  - ✅ 속성 11개(`id nm z a f r p d s t x`), 점수·순위 미포함 (assert 로 강제)
  - ✅ id 유일성 assert — 뒷 8자리는 15,322건 충돌 (D-015)
- [x] **T-402** tippecanoe → `web/data/parcels.pmtiles`
  - ✅ **20.1MB** (상한 50MB), z11~17, 최대줌에 전 필지 52,970 보존
- [x] **T-403** 부가 레이어 — `stations.geojson`(48) / `boundary.geojson` / `meta.json`
  - `meta.json` 에 파라미터 기본값·스케일·플래그 비트·데이터 기준일 포함
- [x] **T-404** 타일 검증 스위트 — `scripts/07_verify_tiles.py`
  - ✅ 완전성 / 속성 왕복 8종 / 타일만으로 재계산 / 계층정렬 상위200 **집합·순서 200/200**
  - **타일을 다시 구운 뒤에는 반드시 실행한다.** 실패 시 종료코드 1

## Phase 5 · 웹 구현

> 디자인은 `.claude/skills/taste-skill/SKILL.md` 의 시각 언어(타이포·컬러 규율·안티슬롭)를
> 따르되, 레이아웃은 지도 대시보드 관례를 쓴다. 색상은 `dataviz` 순차 블루 램프.

- [x] **T-501** 지도 셸 — MapLibre + PMTiles + OSM 래스터 (`web/app.js`)
- [x] **T-502** 브라우저 점수 엔진 (`web/score.js`)
  - ✅ **상위 200 집합·순서 200/200 Python 과 일치** (`tools/verify_ui.mjs`)
  - ✅ `tol=0` 회귀, 후보 수 4,894, 중위 수익률 3.92% 일치
- [x] **T-503** 파라미터 패널 — 슬라이더 11종 + 토글 2종 + 되돌리기
  - ✅ 전체 재계산 7~8ms (목표 200ms)
- [x] **T-504** 필지 클릭 → 상세 패널 (지표·사업구조·제외사유·경고 뱃지)
- [x] **T-505** 상위 후보 리스트 ↔ 지도 연동 (선택 동기화, fitBounds, 스크롤)
- [x] **T-506** "가정 포함" 배지 + 항목별 근거 표기
- [x] **T-507** 반응형(390/820/1920) · 다크모드 · WCAG AA 대비 · reduced-motion
- [x] **T-508** UI 검증 스위트 — `tools/verify_ui.mjs` (실패 시 종료코드 1)
  - 부팅·콘솔에러 / Python 대조 / 파라미터 / 선택·상세 / 다크모드·대비 / 반응형·겹침

## Phase 6 · 배포

- [x] **T-601** GitHub Pages 배포 — Actions 워크플로 (`.github/workflows/pages.yml`)
  - `web/` 만 게시. 루트에는 스크립트·문서가 있어 Pages 기본 옵션(루트//docs)이 맞지 않는다
  - 배포 전 산출물 존재·비어있지 않음 점검 포함
  - **https://cho-woojin.github.io/dormitory-site-selection/** · 과금 서비스 0개
- [x] **T-602** PMTiles Range 요청 실배포 확인
  - ✅ `HTTP/2 206` + `accept-ranges: bytes` + `content-range: bytes 100-200/20134517`
  - ✅ 배포본에서 PMTiles 헤더 정상 판독 (v3, 줌 11~17)
- [x] **T-603** README 작성 — 방법론·결과·아키텍처·검증·한계 + 정릉동 캡처 11장
  - 웹에도 "데이터 출처와 방법론" 패널 추가
- [x] **T-604** 출처·기준연도·라이선스 표기
  - SGIS 경계는 **CC BY 4.0 이라 표기가 의무**다. 출처 패널 + 지도 attribution 양쪽에 명시
  - attribution 이 패널에 가려지지 않는지도 검증 항목으로 추가
- [x] **T-605** 배포본 대상 전체 검증 통과 (`node tools/verify_ui.mjs <배포URL>`)

---

## 지금 막혀 있는 것

**데이터는 전부 확보됐다.** 이제 구현만 남았다.

**전 Phase 완료 + 성동구 확장 완료.**

## Phase 7 · 성동구 확장 (2026-09-01)

- [x] **T-701** `region.districts` 다중 자치구 구조로 전환 (D-018)
- [x] **T-702** 성동구 데이터 확보 — 26,312 필지 / 건물 25,074동
- [x] **T-703** 파이프라인 전체 재실행 — 79,282 필지 → 후보 8,112
  - ✅ **성북구 결과 불변** (후보 4,894, 수익률 3.92%) — 회귀 통과
- [x] **T-704** 일조 경계 효과 보정 — 건물을 대상 구 + 버퍼 120m 로 읽음 (73,027동)
- [x] **T-705** 웹 자치구 필터 + 비교 왜곡 경고 (D-019)
- [x] **T-706** 검증 스위트 전 항목 통과 (타일·UI 양쪽)

### 확장하면서 잡은 버그

| 증상 | 원인 | 대응 |
|------|------|------|
| 타일 id 892건 충돌 | PNU 에서 시군구를 떼면 법정동 일련번호가 구마다 반복 | 19자리 전체 사용 |
| 성동구 철거비가 전부 0 | `buildings.gpkg` 캐시가 성북구만 담은 채 재사용 | 캐시 정합성 검증 추가 |
| 검증이 공허하게 통과 | 조인이 비어도 "불일치 0" 이 된다 | 조인 건수 검증 선행 |
| **자연림이 3위** | 지목 '대' 인 응봉산 자락. `excluded_landuse` 누락 | 산림·농지·유해시설 추가 |
| 유해·혐오시설이 18위 | 위와 동일 | 위와 동일 |
| 성동구 필지명에 '서울특별시 성동구' | 이름 정리가 '성북구' 하드코딩 | 시도명만 제거 |

---

## 다음에 손댈 만한 것

1. **Q-13 자치구별 임대료.** 지금은 단일값이라 자치구 간 비교가 성립하지 않는다 (D-019).
   근거 있는 구별 원룸·기숙사 시세를 확보하면 가장 큰 개선이다.
2. **Q-14 준공업지역 기숙사 가부.** 성동구 후보의 36%가 준공업지역(성수동)이다.
   서울시 준공업지역 산업기능 보호 정책 확인이 필요하다.
3. **가정값 보정.** 단위공사비·전용률·공실률·실거래배율.
   `config/params.yaml` 한 곳만 고치고 파이프라인을 다시 돌리면 된다.
4. **도보 네트워크 거리.** 지금은 직선거리다 (`transit.distance_metric: walking` 자리는 비워 뒀다).
5. **상업지역 비주거 의무비율**(연면적 10%) 반영 검토.

## 아직 검증되지 않은 가정

결과를 의사결정에 쓰기 전에 반드시 확인해야 한다:

| 파라미터 | 현재값 | 상태 |
|----------|--------|------|
| 단위공사비 | 250만원/㎡ | **가정** — 임대형기숙사 실제 시공 견적 대조 필요 |
| 전용률 | 0.65 | **가정** |
| 실당 면적 | 16㎡ | **가정** |
| 공실률 / 운영비율 | 0.05 / 0.25 | **가정** |
| 실거래배율 | 1.5 | **가정** (통상 1.4~1.7) |
| 실현계수 base | 0.90 | **가정** (형상·경사 계수는 실측) |
| 실당 월임대료 / 보증금 | 66만 / 1,000만원 | ✅ 2026년 성북구 시세 |
| 용적률·건폐율 | 조례표 | ✅ 조례 원문 |
| 용도지역·형상·경사·공시지가·접도 | — | ✅ 토지특성정보 실측 |
| 상업지역 주거용 400% 상한 | — | ✅ 조례 별표 (D-011) |
