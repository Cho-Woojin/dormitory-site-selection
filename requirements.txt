# 공간분석
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rtree>=1.3
fiona>=1.10
pyogrio>=0.9

# 래스터 (DEM / 경사도)
rasterio>=1.3
elevation>=1.1

# 도로망 (OSM 대안 경로 — DATA_SOURCES 2.2)
osmnx>=2.0

# 수치·데이터
numpy>=1.26
pandas>=2.2
pyarrow>=15.0

# 설정·수집
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0

# 검증용 (선택)
matplotlib>=3.8
